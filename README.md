# compilateur algorithmique :

## tester le compilateur (sous linux):

**Compilation :**

```bash
$ chmod +x build.sh # donner permission d'execution
$ ./build.sh compiler # compiler nom de compilateur (Vous pouvez le changer)
```
**Tester :**
```code
$ ./compiler code.txt
```